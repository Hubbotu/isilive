# Worklog 2026-04-17

Logs in Dateien: WoW-Addons können nicht direkt Dateien schreiben. Der Ring-Buffer landet in IsiLiveDB (SavedVariables), also in WTF/.../isiLive.lua — aber erst beim Logout/Reload. Das ändert sich durch den Tab nicht.

Tab öffnet automatisch: Nein — nur wenn /isilive log watch aktiviert wird. Beim Addon-Load nichts.

Nur befüllt wenn Log aktiv: Ja, automatisch — der Watch-Callback wird nur aus AppendLog aufgerufen, und der wird nur aufgerufen wenn IsLevelEnabled true ist. Log off = kein Eintrag = kein Tab-Output.

Zur Architektur — sauberer Weg:

Neues Modul ui/isiLive_trace_chat_frame.lua mit CreateTraceChatFrame() → gibt Controller zurück (Open, AddMessage, IsOpen)
WoW-API-Aufrufe nur dort, kein direktes Frame-Handling in commands.lua
Controller wird in factory_controllers.lua instanziiert und via ctx durchgereicht
BuildSlashCommandsOpts reicht traceChatFrame weiter
commands.lua bekommt es als injizierbares Dep → testbar mit Mock

